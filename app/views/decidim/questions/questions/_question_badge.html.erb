<% if question.published_state? || question.withdrawn? %>
  <span class="<%= question_state_badge_css_class(question.state) %> label question-status"><%= humanize_question_state question.state %></span>
<% end %>
